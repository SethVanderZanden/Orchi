export {
  filterProjectGroups as filterWorkspaceGroups,
  groupChatsByProject as groupChatsByWorkspace,
  ORPHAN_GROUP_ID,
  type ProjectChatGroup as WorkspaceChatGroup
} from '@/lib/projects/group-chats'
